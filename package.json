{
  "name": "dsh-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "DeepSeek Harness desktop shell (Tauri)",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:deb": "tauri build --bundles deb",
    "build:appimage": "tauri build --bundles appimage"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
